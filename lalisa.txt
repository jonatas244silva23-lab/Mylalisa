<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para a Lalisa ❤️</title>
    
    <!-- Importando fontes bonitas do Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Montserrat:wght@300;500;700&display=swap" rel="stylesheet">
    
    <style>
        /* CSS INTERNO - ESTILO DO SITE */
        :root {
            --primary-color: #ff6b81;
            --secondary-color: #ff9eb5;
            --bg-gradient: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
            --glass-bg: rgba(255, 255, 255, 0.85);
            --text-color: #4a4a4a;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(120deg, #f6d365 0%, #fda085 100%);
            background: radial-gradient(circle, rgba(255,228,230,1) 0%, rgba(255,182,193,1) 100%);
            min-height: 100vh;
            color: var(--text-color);
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
        }

        /* Efeito de Chuva de Corações */
        .heart-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }

        .heart {
            position: absolute;
            bottom: -50px;
            color: rgba(255, 255, 255, 0.6);
            animation: floatUp linear infinite;
        }

        @keyframes floatUp {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-120vh) rotate(360deg); opacity: 0; }
        }

        /* Container Principal */
        .container {
            width: 90%;
            max-width: 600px;
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 30px;
            margin: 40px auto;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            z-index: 10;
            position: relative;
            border: 2px solid white;
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            font-size: 3.5rem;
            color: var(--primary-color);
            margin-bottom: 20px;
            text-shadow: 2px 2px 0px rgba(255,255,255,0.5);
            animation: fadeInDown 1.5s ease;
        }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 20px;
            color: #555;
        }

        .quote {
            font-style: italic;
            color: var(--primary-color);
            font-weight: 500;
            margin: 25px 0;
            display: block;
            border-left: 4px solid var(--secondary-color);
            padding-left: 15px;
            text-align: left;
            background: rgba(255,255,255,0.5);
            padding: 10px;
            border-radius: 0 10px 10px 0;
        }

        /* Vídeos */
        .video-container {
            position: relative;
            padding-bottom: 56.25%; /* 16:9 */
            height: 0;
            margin-bottom: 20px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            border: 5px solid white;
        }

        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        /* Proposta */
        .proposal-box {
            background: #fff0f3;
            padding: 25px;
            border-radius: 15px;
            border: 2px dashed var(--primary-color);
            margin-top: 30px;
            animation: pulse 3s infinite;
        }

        .proposal-text {
            font-size: 1.3rem;
            font-weight: 600;
            color: #d63031;
        }

        /* Botões */
        .buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 25px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 40px;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .btn-yes {
            background: var(--primary-color);
            color: white;
        }

        .btn-yes:hover {
            background: #ff4757;
            transform: scale(1.05);
            box-shadow: 0 0 20px rgba(255, 107, 129, 0.6);
        }

        .btn-no {
            background: #e0e0e0;
            color: #777;
        }

        .btn-no:hover {
            background: #d1d1d1;
            transform: scale(0.95);
        }

        /* Controle de Música */
        .music-control {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 100;
            background: white;
            padding: 10px;
            border-radius: 50%;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            cursor: pointer;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            transition: transform 0.3s;
        }
        
        .music-control:hover {
            transform: rotate(15deg);
        }

        /* Modal de Resposta */
        #responseModal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background: white;
            padding: 30px;
            border-radius: 20px;
            width: 90%;
            max-width: 400px;
            text-align: center;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
            animation: zoomIn 0.3s ease;
        }

        textarea {
            width: 100%;
            height: 100px;
            padding: 10px;
            margin: 15px 0;
            border: 2px solid #eee;
            border-radius: 10px;
            font-family: 'Montserrat', sans-serif;
            resize: none;
        }

        textarea:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .final-message {
            display: none;
            text-align: center;
            margin-top: 50px;
        }

        /* Animações */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.5); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Ocultar player de audio padrão */
        audio { display: none; }

    </style>
</head>
<body>

    <!-- Música de Fundo -->
    <audio id="bgMusic" loop>
        <source src="https://cdn.pixabay.com/download/audio/2022/03/24/audio_3047715f3e.mp3?filename=romantic-piano-111585.mp3" type="audio/mpeg">
    </audio>

    <!-- Botão de Música -->
    <div class="music-control" onclick="toggleMusic()" title="Ligar/Desligar Música">
        <span id="musicIcon">🔇</span>
    </div>

    <!-- Fundo com Corações (JS vai preencher) -->
    <div class="heart-bg" id="heartBg"></div>

    <div class="container" id="mainContainer">
        <h1>Para minha Lalisa✨</h1>
        
        <p>Desde que você apareceu, as cores ficaram mais vivas e os dias mais bonitos.</p>
        
        <div class="quote">
            "O meu mundo é horrível, mas ele ficou muito melhor quando você chegou."
        </div>

        <!-- Vídeo Fofo -->
        <div class="video-container">
            <iframe width="560" height="315" src="https://files.catbox.moe/wqyc9j.mp4" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
        </div>

        <p>Eu estava guardando essas palavras no peito, esperando o momento certo. Acho que não existe momento perfeito, existe apenas... <b>nós</b>.</p>

        <div class="quote">
            "Você é a minha notificação favorita e o meu pensamento mais doce antes de dormir."
        </div>

        <!-- Seção do Pedido -->
        <div class="proposal-box">
            <p class="proposal-text">
                “Lalisa, cada detalhe seu ilumina meu mundo. Hoje eu criei esse cantinho só pra dizer o quanto você é especial para mim. <br><br>
                Então… você aceita namorar comigo?”
            </p>

            <div class="buttons">
                <button class="btn btn-yes" onclick="openModal('Sim')">Sim 💗</button>
                <button class="btn btn-no" onclick="openModal('Não')">Não 😢</button>
            </div>
        </div>
        
        <p style="margin-top: 30px; font-size: 0.8rem; color: #888;">Feito com todo o ❤️ do mundo.</p>
    </div>

    <!-- Modal para Mensagem -->
    <div id="responseModal">
        <div class="modal-content">
            <h2 id="modalTitle" style="color: var(--primary-color); margin-bottom: 10px;"></h2>
            <p>Deixe um recadinho (opcional) e clique em enviar para abrir meu WhatsApp:</p>
            <textarea id="userMessage" placeholder="Escreva algo fofo aqui... (ou deixe em branco)"></textarea>
            <button class="btn btn-yes" onclick="sendMessage()">Enviar Resposta 💌</button>
            <br>
            <button onclick="closeModal()" style="background:none; border:none; color:#888; margin-top:10px; cursor:pointer; text-decoration:underline;">Cancelar</button>
        </div>
    </div>

    <!-- Mensagem Final -->
    <div class="container final-message" id="finalMessage">
        <h1>Obrigado! 💖</h1>
        <p>Sua resposta está sendo enviada para o meu WhatsApp. Meu coração está disparado agora!</p>
        <div class="video-container">
            <!-- Vídeo de comemoração -->
            <iframe width="560" height="315" src="https://files.catbox.moe/pmu12q.mp4" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
        </div>
        <p>Eu Te amo, Lalisa.</p>
    </div>

    <script>
        // --- Lógica da Música ---
        const audio = document.getElementById('bgMusic');
        const musicIcon = document.getElementById('musicIcon');
        let isPlaying = false;

        function toggleMusic() {
            if (isPlaying) {
                audio.pause();
                musicIcon.innerText = "🔇";
            } else {
                audio.play().catch(e => console.log("Interaja primeiro para tocar"));
                musicIcon.innerText = "🎵";
            }
            isPlaying = !isPlaying;
        }

        // --- Efeito de Corações Flutuantes ---
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.fontSize = Math.random() * 20 + 10 + 'px';
            heart.style.animationDuration = Math.random() * 3 + 2 + 's';
            
            document.getElementById('heartBg').appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 5000);
        }

        setInterval(createHeart, 300);

        // --- Lógica do Pedido e WhatsApp ---
        const phoneNumber = "5561992977672"; 
        let currentAnswer = "";

        function openModal(answer) {
            const modal = document.getElementById('responseModal');
            const title = document.getElementById('modalTitle');
            const textarea = document.getElementById('userMessage');
            
            currentAnswer = answer;
            modal.style.display = 'flex';
            
            if(answer === 'Sim') {
                title.innerText = "Obaaaa! 😍";
                textarea.placeholder = "Quer escrever algo a mais? Se não, é só clicar em enviar!";
                // Efeito extra de corações
                for(let i=0; i<30; i++) setTimeout(createHeart, i*100);
            } else {
                title.innerText = "Ah, poxa... 😢";
                textarea.placeholder = "Pode me dizer o porquê? (Opcional)";
            }
        }

        function closeModal() {
            document.getElementById('responseModal').style.display = 'none';
        }

        function sendMessage() {
            // Pega o texto que ela digitou (se digitou algo)
            const userMsg = document.getElementById('userMessage').value;
            
            let finalMessage = "";

            // Cria a mensagem baseada na escolha dela
            if (currentAnswer === "Sim") {
                finalMessage = `Oiii! Aqui é a Lalisa. 🥰\n\nEu vi o site lindo que você fez e minha resposta é: *SIM, EU ACEITO!* 💍💗\n\n${userMsg}`;
            } else {
                finalMessage = `Oi... Aqui é a Lalisa.\n\nEu vi o site. Minha resposta é: *Não* 😢\n\n${userMsg}`;
            }

            // Formata para URL do WhatsApp
            const encodedMessage = encodeURIComponent(finalMessage);
            const waLink = `https://wa.me/${phoneNumber}?text=${encodedMessage}`;

            // Abre o WhatsApp imediatamente em uma nova aba
            window.open(waLink, '_blank');

            // Atualiza a tela do site para a mensagem de agradecimento
            document.getElementById('responseModal').style.display = 'none';
            document.getElementById('mainContainer').style.display = 'none';
            document.getElementById('finalMessage').style.display = 'block';
            
            // Se a música não estiver tocando, liga ela para celebrar
            if(!isPlaying) {
                toggleMusic();
            }
        }

        // Fecha o modal se clicar fora dele
        window.onclick = function(event) {
            const modal = document.getElementById('responseModal');
            if (event.target == modal) {
                closeModal();
            }
        }
    </script>
</body>
</html>